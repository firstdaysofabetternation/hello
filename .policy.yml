policy:
  approval:
    - security and dev

    

approval_rules:
  - name: security and dev
    requires:
      count: 1
      teams: ["firstdaysofabetternation/security","firstdaysofabetternation/dev"]
    options:
      request_review:
        enabled: true
        mode: teams
